'''

Q5. Create an empty dictionary. Allow 4 friends to enter their
    favourite language as value and use key as their names.
    Assume that the names are unique.

'''

d={}
name=input("Enter the name: ")
language=input("Enter the language: ")
d.update({name:language})

name=input("Enter the name: ")
language=input("Enter the language: ")
d.update({name:language})

name=input("Enter the name: ")
language=input("Enter the language: ")
d.update({name:language})

name=input("Enter the name: ")
language=input("Enter the language: ")
d.update({name:language})

print(d)



'''

As we know we should enter unique values as set cancels reccuring values,


'''